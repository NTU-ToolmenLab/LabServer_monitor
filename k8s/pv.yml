apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-monitor
spec:
  storageClassName: nfs-monitor
  persistentVolumeReclaimPolicy: Delete
  capacity:
    storage: 10Gi
  accessModes:
  - ReadWriteMany
  nfs:
    server: $IP_NAS
    path: $NAS_MONITOR_PATH
